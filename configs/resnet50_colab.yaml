seed: 42
model: resnet50
image_size: 224
batch_size: 64          # larger batch uses GPU memory efficiently
num_workers: 2
max_epochs: 10          # adjust if you want a longer run
lr: 0.0003
weight_decay: 0.0001
label_smoothing: 0.05
use_class_weights: true
train_csv: data/processed/train.csv
val_csv: data/processed/val.csv
out_dir: runs/resnet50_colab
